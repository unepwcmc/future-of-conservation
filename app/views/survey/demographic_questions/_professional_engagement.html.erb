<p class="card-text">
  <%= radio_button_tag "demographics[#{question.short_name}]", 'Exclusively a Researcher' %>
  <%= label_tag 'Exclusively a Researcher' %>
</p>
<p class="card-text">
  <%= radio_button_tag "demographics[#{question.short_name}]", 'Mostly a Researcher' %>
  <%= label_tag 'Mostly a Researcher' %>
</p>
<p class="card-text">
  <%= radio_button_tag "demographics[#{question.short_name}]", 'Both a Researcher and Practictioner' %>
  <%= label_tag 'Both a Researcher and Practictioner' %>
</p>
<p class="card-text">
  <%= radio_button_tag "demographics[#{question.short_name}]", 'Mostly a Practitioner' %>
  <%= label_tag 'Mostly a Practitioner' %>
</p>
<p class="card-text">
  <%= radio_button_tag "demographics[#{question.short_name}]", 'Exclusively a Researcher' %>
  <%= label_tag 'Exclusively a Researcher' %>
</p>
