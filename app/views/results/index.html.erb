<div class="row my-2">
<div class="col-lg-12">
  <h3 class="my-2">Showing <%= @results.count %> results</h3>
  <table class="table">
    <thead>
      <th>ID</th>
      <th>Classification</th>
      <th>Created At</td>
      <th>View</th>
    </thead>
    <tbody>
      <% @results.each do |answer_set| %>
        <tr>
          <td><%= answer_set.id %></td>
          <td><%= answer_set.classification.name %></td>
          <td><%= answer_set.created_at.to_formatted_s(:long) %></td>
          <td><%= link_to 'View Results', results_path(answer_set.uuid) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>
