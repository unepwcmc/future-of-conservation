<div class="row my-2">
<div class="col-lg-12">
  <h3 class="my-2">Showing <%= @results.count %> results</h3>

  <button type="button" class="btn btn-primary my-2" data-toggle="modal" data-target="#exportModal">
    Export results as .csv
  </button>

  <div class="modal fade" id="exportModal" tabindex="-1" role="dialog" aria-labelledby="exportModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exportModalLabel">Export CSV</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <%= form_tag(export_results_path, method: "post") do %>
        <div class="modal-body">
          <%= label_tag(:q, "Date range (from/to):") %>
          <div class="form-group">
            <%= date_field(:q, :from_date, value: Date.new(2016, 1, 1), class: "form-control") %>
            <%= date_field(:q, :to_date, value: Date.today, class: "form-control") %>
          </div>
          <%= label_tag(:q, "Email address:") %>
          <div class="form-group">
          <%= text_field(:q, :to_email, size: 50, required: true, class: "form-control") %>
          </div>
        </div>
        <div class="modal-footer">
          <%= submit_tag("Filter and Export Results", class: 'btn btn-primary my-2') %>
        </div>
        <% end %>
      </div>
    </div>
  </div>

  <table class="table">
    <thead>
      <th>ID</th>
      <th>Classification</th>
      <th>Created At</td>
      <th>View</th>
    </thead>
    <tbody>
      <% @results.each do |answer_set| %>
        <tr>
          <td><%= answer_set.id %></td>
          <td><%= answer_set.classification.name %></td>
          <td><%= answer_set.created_at.to_formatted_s(:long) %></td>
          <td><%= link_to 'View Results Page', results_path(answer_set.uuid) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>
